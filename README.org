[![Review Assignment Due Date](https://classroom.github.com/assets/deadline-readme-button-22041afd0340ce965d47ae6ef1cefeee28c7c493a6346c4f15d667ab976d596c.svg)](https://classroom.github.com/a/FTvLdgi7)
#+TITLE: ES118 Lab #6 Assignment
#+AUTHOR: Ufuk Baler, MSc. & Assoc. Prof. Dr. Fethi Okyar
#+SUBTITLE: 
#+STARTUP: overview
#+REVEAL_THEME: simple
#+REVEAL_INIT_OPTIONS: slideNumber:"c/t", width:1920, height:1080, navigationMode:"linear", transition:"none"
#+REVEAL_TITLE_SLIDE: <h1>%t</h1> <h2>%s</h2> <h5>%a</h5>
#+OPTIONS: timestamp:nil toc:1 num:nil reveal_global_footer:nil
#+REVEAL_EXTRA_CSS: ../style.css
#+LATEX_HEADER: \usepackage{amsmath}

* Definition of some arrays
Create ~lab6.py~ and write a function called ~arrays~ which will have no arguments. It will create and return the given arrays as ~array1~, ~array2~, ~array3~, ~array4~, ~array5~, ~array6~, ~array7~, ~array8~, ~array9~, ~array10~, respectively from top to bottom.

The arrays ~array1~, ~array2~ and ~array10~ shall be created manually using ~np.array~ function. The others should be done using special functions given in the lecture.

#+ATTR_HTML: :width 150px
[[./arrays.png]]



* A flight route
#+ATTR_HTML: :width 350px
[[./vectors.png]]

The origin of the coordinate system represents the center of a plane. The plane has done three consecutive displacements shown with respect to the center of the plane: $\vec v_1$, $\vec v_2$ and $\vec v_3$. Its resulting displacement is given by $\vec v_R$.

A Python program is needed to show its final position in terms of the given three displacements. The inputs are six individual components $v_1^x$, $v_1^y$, $v_2^x$, $v_2^y$, $v_3^x$, $v_3^y$ of the displacement vectors in km. Construct three displacement vectors and sum those such that $\vec v_R$ is computed.

Also find the projection of $\vec v_R$ on $\vec u$ using $\vec v_R^u = (\vec v_R \cdot \vec u) \vec u$.

Lastly, return the $\vec v_R$ as a NumPy array and the length of $\vec v_R^u$.

Create a new function called ~total_displacement~ in ~lab6.py~ to solve this problem.

For vector definitions and other special functions use NumPy.


